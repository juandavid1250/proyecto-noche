#API REST Python

proyecto de api rest desarrollado con python


## Ejecutar 

1. Instala dependencias:
   `pip install -r requirements.txt`
2. Ejecuta la API:
   `python main.py`
3. Accede a la documentación:
   `http://localhost:8000/docs`

El frontend puede conectarse al backend en `http://localhost:8000`.
